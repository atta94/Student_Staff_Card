<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student_staff_card">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Staff_Detail"></activity>
        <activity android:name=".Student_Staff_Home">
            </activity>
        <activity android:name=".Student_Detail" />
        <activity android:name=".Login" />
        <activity android:name=".Staff_Reg" />
        <activity android:name=".Main_Screen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Student_Reg" />
    </application>

</manifest>